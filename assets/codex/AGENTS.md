# Global preferences

- Respond in Simplified Chinese by default, including progress updates and final answers.
- Preserve the original language for code, commands, identifiers, file paths, logs, error messages, and technical terms when translation would reduce precision.
- Follow the user's explicit language request when they ask for a language other than Chinese.
