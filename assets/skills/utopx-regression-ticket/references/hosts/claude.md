# Claude Code adapter

Read the sprint resource with:

```text
ReadMcpResourceTool(server="nvidia-redmine", uri="redmine://projects/103/sprints")
```

If the server is not connected, follow the REST fallback referenced by the shared skill.
