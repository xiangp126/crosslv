# Codex adapter

Search Codex's deferred tool catalogue before concluding an MCP is absent. The normal active set
does not include `nvidia-confluence`; use `nvidia-glean` for indexed prose discovery and
`confluence-cli` for exact page metadata, hierarchy, attachments, and exports.

Do not add or authenticate another MCP merely to complete a read that Glean or the CLI can
perform. Agent writes use `~/myGit/crosslv/assets/aipim/confluence-update`.
