# ~/.bash_profile - executed for login shells (macOS, tmux, SSH)
# Works on both macOS and Linux

# Source .bashrc if it exists (for aliases, functions, prompt, etc.)
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi
